# Items for the QA pass (collected from build reports)

- Posts list: Queue tab ordered by updatedAt desc — should be soonest target first (pagination can hide near posts). (B4a)
- Media DELETE 409 check ignores SocialPostTarget.mediaOverride (Json). (B2)
- MediaResolverService.stream() buffers whole file; publishers must use ranged reads (B3 says chunk-source does). Verify no caller streams 1 GB into memory. (B2/B3)
- POST /social-accounts/:id/refresh only refreshes token, not profile/avatar. (B2)
- Frontend Instagram connect shows two buttons that hit the same flow; backend picks Instagram Login when INSTAGRAM_APP_ID is set. (F2b)
- Frontend isAdmin for WhatsApp manual / Twilio sync uses isOwner (not ADMIN_ROLES); backend enforces. (F2b)
- Inbox assignee: FE sends User ids; backend must translate to TenantUser (rule in CONTRACT §3.7). (F3a/B-inbox)
- commentScore = 100 when there are no comments in 30 days. (B4a)
- Calendar tz math is hand-rolled with Intl; may be ±1 h on DST week. (F1b)
- Inbox attachments from Messenger/IG/X/Bluesky are stored as the platform CDN URL — Meta CDN URLs expire (hours/days), so old attachments will break. Should copy to the private bucket like MMS. (B5)
- WhatsApp via Twilio treated as always-open; WhatsApp enforces the 24 h session window through Twilio too (outside it needs an approved template → error 63016). (B5)
- DM pollers re-fetch the last ~20 events per cycle with no cursor (idempotent on externalId, so safe, just wasteful). (B5)
